{
  "name": "frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node prepare-deploy.js && gh-pages -d ../dist/public"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.10.29",
    "gh-pages": "^6.3.0"
  }
}
