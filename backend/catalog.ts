// Re-exports from shared/catalog.ts — kept here so existing backend imports
// (db.ts, shop.test.ts, seed.ts) continue to work unchanged.
export { CatalogProduct, initialCatalog, catalogById } from "../shared/catalog";
